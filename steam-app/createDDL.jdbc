CREATE TABLE STEAM.MARKETGAME (ID INTEGER NOT NULL, GAMENAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE STEAM.MARKETITEM (URL VARCHAR(255), GAMEID INTEGER NOT NULL, ITEMNAME VARCHAR(255) NOT NULL, PRIMARY KEY (GAMEID, ITEMNAME))
CREATE TABLE STEAM.MARKETLISTING (ID VARCHAR(255) NOT NULL, TIMESTAMP TIMESTAMP, GAMEID INTEGER, ITEMNAME VARCHAR(255), CURRENCY VARCHAR(255), PRICEWITHFEE FLOAT, PRIMARY KEY (ID))
CREATE TABLE steam.WATCHEDMARKETITEM (CREATEDTIMESTAMP TIMESTAMP, JOBID VARCHAR(255), GAMEID INTEGER NOT NULL, ITEMNAME VARCHAR(255) NOT NULL, PRIMARY KEY (GAMEID, ITEMNAME))
ALTER TABLE STEAM.MARKETLISTING ADD CONSTRAINT MRKETLISTINGGAMEID FOREIGN KEY (GAMEID, ITEMNAME) REFERENCES STEAM.MARKETITEM (GAMEID, ITEMNAME)
